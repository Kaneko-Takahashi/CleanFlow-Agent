# CleanFlow Agent（LLM連携用）
# 今はダミーファイルとして作成
# 後でLLMロジックを実装する

def generate_plan(profile: dict, task_type: str, target_column: str = None) -> dict:
    """前処理プランを生成（ダミー実装）"""
    # 後でLLMを使って実装
    return {
        "steps": []
    }

